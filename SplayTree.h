#include <string>
#include <iostream>
#include <fstream>
#include <vector>
#include <list>
#include <algorithm>

using namespace std;

class SplayTree
{
private:
    int test;
//
public:
    SplayTree( int set );
    int getTest() { return test; }
};